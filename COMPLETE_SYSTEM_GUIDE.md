# 완전한 크롤러 시스템 가이드

## 🎯 시스템 개요

차단 방지, 데이터 구조 관리, 다양한 수집 시나리오를 통합한 완전한 크롤러 시스템입니다.

## ✅ 구현된 기능

### 1. 페이크 페이스 시스템 (`fake_face_system.py`)

**목적**: 무차별적 사이트 로그 시 차단 위험 방지

**기능**:
- ✅ 인간처럼 행동하는 크롤러
- ✅ 4가지 프로필 (casual, researcher, quick_scan, thorough)
- ✅ 랜덤 대기 시간 (프로필별 최소/최대)
- ✅ 읽기 시뮬레이션
- ✅ 스크롤 시뮬레이션
- ✅ 세션 관리 및 휴식
- ✅ 랜덤 User-Agent 로테이션
- ✅ Referer 헤더 관리

**프로필**:
- `casual`: 일반 사용자 (3-8초 대기, 15초 읽기)
- `researcher`: 연구자 (5-15초 대기, 30초 읽기)
- `quick_scan`: 빠른 스캔 (1-3초 대기, 5초 읽기)
- `thorough`: 철저한 검토 (8-20초 대기, 45초 읽기)

### 2. 데이터 구조 템플릿 시스템 (`data_structure_templates.py`)

**목적**: 추출한 데이터의 구조 정의 및 AI 제안

**기능**:
- ✅ 사전 정의된 템플릿 (report_metadata, report_detail, stock_info)
- ✅ 필드 정의 (이름, 타입, 필수 여부, 기본값, 설명)
- ✅ 데이터 검증
- ✅ AI 기반 필드 제안
- ✅ 데이터에서 구조 자동 추출

**템플릿**:
- `report_metadata`: 리포트 메타데이터 (10개 필드)
- `report_detail`: 리포트 상세 정보 (7개 필드)
- `stock_info`: 종목 기본 정보 (5개 필드)

### 3. 크롤링 시나리오 시스템 (`crawling_scenarios.py`)

**목적**: 다양한 수집 전략 제공

**시나리오 타입**:
- `SCHEDULED`: 정기 수집 (일일, 주간 등)
- `KEYWORD_BASED`: 키워드 기반 수집
- `ISSUE_TRACKING`: 이슈 추적
- `FULL_SCAN`: 전체 스캔
- `INCREMENTAL`: 증분 수집
- `TARGETED`: 특정 대상 수집

**사전 정의된 시나리오**:
1. **일일 정기 수집**: 매일 아침 9시, 최근 1일, 최대 50개
2. **주간 전체 스캔**: 매주 월요일, 최근 7일, 최대 500개
3. **키워드 기반 수집**: 특정 키워드 포함, 최근 30일, 최대 200개
4. **이슈 추적**: 이슈/이벤트 관련, 최근 7일, 최대 100개
5. **특정 종목 추적**: 특정 종목만, 최근 90일, 최대 1000개
6. **증분 수집**: 새로 추가된 것만, 최근 1일, 최대 50개
7. **특정 애널리스트 추적**: 특정 애널리스트만, 최근 180일, 최대 500개

### 4. 통합 크롤러 매니저 (`integrated_crawler_manager.py`)

**목적**: 모든 시스템을 통합 관리

**기능**:
- ✅ 페이크 페이스 통합
- ✅ 데이터 구조 관리
- ✅ 시나리오 실행
- ✅ 커스텀 시나리오 생성
- ✅ AI 기반 시나리오 제안
- ✅ 통계 관리

## 🚀 사용 방법

### 기본 사용

```python
from integrated_crawler_manager import IntegratedCrawlerManager

# 매니저 생성
manager = IntegratedCrawlerManager(
    use_fake_face=True,
    fake_face_profile='researcher',
    use_adaptive_parsing=True
)

# 시나리오 실행
result = manager.run_scenario("keyword")
```

### 커스텀 시나리오 생성 및 실행

```python
requirements = {
    'description': '삼성전자 AI 관련 보고서만 수집',
    'days': 30,
    'max_reports': 100,
    'keywords': ['AI', '삼성전자'],
    'stock_codes': ['005930'],
    'use_analysis': True,
    'fake_face_profile': 'researcher'
}

result = manager.create_and_run("삼성전자 AI 추적", requirements)
```

### 시나리오 제안 받기

```python
requirements = {
    'keywords': ['AI', '반도체'],
    'days': 7
}

suggestions = manager.suggest_scenarios(requirements)
for sug in suggestions:
    print(f"{sug['name']}: {sug['description']}")
```

### 데이터 구조 사용

```python
# 템플릿 가져오기
template = manager.get_data_structure("report_metadata")

# 필드 제안
data = {"report_id": "RPT_001", "title": "..."}
suggestions = manager.suggest_fields(data, "report_metadata")

# 데이터 검증
is_valid, errors = manager.validate_data(data, "report_metadata")
```

## 📊 대시보드 사용

### 실행

```bash
python run_complete_dashboard.py
```

### 대시보드 기능

1. **모니터링 탭**
   - 실시간 크롤러 상태
   - 사이트 건강도
   - 활동 로그

2. **시나리오 탭**
   - 사전 정의된 시나리오 목록
   - 시나리오 실행
   - 커스텀 시나리오 생성

3. **데이터 구조 탭**
   - 템플릿 보기
   - 필드 제안
   - 데이터 검증

4. **페이크 페이스 탭**
   - 프로필 선택
   - 세션 통계
   - 설정 관리

## 🎯 시나리오 예시

### 예시 1: 일일 정기 수집

```python
result = manager.run_scenario("daily")
# 매일 아침 9시에 최근 1일 보고서 수집
```

### 예시 2: 키워드 기반 수집

```python
result = manager.run_scenario("keyword")
# AI, 반도체, HBM 키워드가 포함된 보고서만 수집
```

### 예시 3: 특정 종목 추적

```python
result = manager.run_scenario("stock")
# 삼성전자(005930), SK하이닉스(000660)의 모든 보고서 수집
```

### 예시 4: 커스텀 시나리오

```python
requirements = {
    'keywords': ['실적 발표', '목표가 상향'],
    'days': 7,
    'max_reports': 50,
    'fake_face_profile': 'thorough'
}

result = manager.create_and_run("실적 발표 추적", requirements)
```

## 🛡️ 차단 방지 전략

### 페이크 페이스 프로필 선택

- **일반 수집**: `casual` (3-8초 대기)
- **키워드 검색**: `researcher` (5-15초 대기)
- **빠른 스캔**: `quick_scan` (1-3초 대기) - 주의 필요
- **철저한 검토**: `thorough` (8-20초 대기) - 가장 안전

### 권장 사항

1. **대량 수집 시**: `thorough` 프로필 사용
2. **정기 수집**: `casual` 또는 `researcher` 프로필
3. **빠른 확인**: `quick_scan` 프로필 (제한적 사용)
4. **세션 관리**: 자동 휴식 기능 활용
5. **User-Agent**: 자동 로테이션 활성화

## 📋 데이터 구조 템플릿

### 리포트 메타데이터 템플릿

```python
{
    "report_id": "string (필수)",
    "title": "string (필수)",
    "stock_code": "string (필수)",
    "stock_name": "string (필수)",
    "analyst_name": "string (필수)",
    "firm": "string (필수)",
    "published_date": "date (필수)",
    "source_url": "string (필수)",
    "investment_opinion": "string (선택)",
    "target_price": "number (선택)"
}
```

### 필드 제안 예시

```python
data = {"report_id": "RPT_001", "title": "삼성전자 리포트"}

suggestions = manager.suggest_fields(data, "report_metadata")
# 누락된 필수 필드와 유용한 선택 필드 제안
```

## 📁 파일 구조

```
crawler_01/
├── fake_face_system.py              # 페이크 페이스 시스템
├── data_structure_templates.py      # 데이터 구조 템플릿
├── crawling_scenarios.py            # 크롤링 시나리오
├── integrated_crawler_manager.py    # 통합 매니저
├── run_complete_dashboard.py       # 완전한 대시보드
└── COMPLETE_SYSTEM_GUIDE.md        # 이 문서
```

## 🔧 고급 설정

### 페이크 페이스 커스터마이징

```python
from fake_face_system import HumanBehaviorProfile, FakeFaceSystem

# 커스텀 프로필 생성
custom_profile = HumanBehaviorProfile(
    name='custom',
    min_delay=4.0,
    max_delay=10.0,
    scroll_probability=0.7,
    click_probability=0.5,
    reading_time_per_page=20.0,
    session_duration=3600.0,
    break_probability=0.3,
    break_duration=600.0
)

# 프로필 등록
FakeFaceSystem.PROFILES['custom'] = custom_profile
```

### 커스텀 템플릿 생성

```python
from data_structure_templates import DataStructureTemplate, FieldDefinition

template = DataStructureTemplate(
    name="custom_template",
    description="커스텀 템플릿",
    fields=[
        FieldDefinition("field1", "string", True, "", "설명"),
        FieldDefinition("field2", "number", False, 0, "설명")
    ]
)

manager.data_manager.register_template(template)
```

## ⚠️ 주의사항

1. **차단 위험**: `quick_scan` 프로필은 제한적으로 사용
2. **대량 수집**: `thorough` 프로필 권장
3. **세션 관리**: 자동 휴식 기능 활용
4. **데이터 검증**: 수집 후 템플릿으로 검증 권장
5. **시나리오 테스트**: 실제 실행 전 작은 범위로 테스트

## 📈 성능 최적화

1. **프로필 선택**: 용도에 맞는 프로필 선택
2. **시나리오 최적화**: 필요한 범위만 수집
3. **필터 활용**: 키워드/종목 필터로 불필요한 수집 방지
4. **증분 수집**: `incremental` 시나리오 활용

## 🎉 완료된 기능

- ✅ 페이크 페이스 시스템 (4가지 프로필)
- ✅ 데이터 구조 템플릿 (3가지 템플릿)
- ✅ 크롤링 시나리오 (7가지 시나리오)
- ✅ 통합 매니저
- ✅ 완전한 대시보드
- ✅ AI 기반 제안 시스템

---

**완료일**: 2025-12-30
**상태**: ✅ 모든 시스템 정상 작동


